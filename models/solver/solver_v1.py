import torch
import torch.nn as nn
import torch.nn.functional as F
import torch.nn.utils.prune as prune
import numpy as np

def dropout_solver(x):
    p = 0.    
    return p

def pruning_solver(x):
    p = 0.
    return p